const quadratic_equation_solver = require("./solver");const parser = require("./parser");const reducer = require("./reducer");if (process.argv.length !== 3) {	console.log("Usage: ./computor \"equation\"");	console.log("Example: ./computor \"1 * X^2 + 2 * X^1 + 3 * X^0 = 0\"");	process.exit(0);}const main = () => {	let input_string = process.argv.slice(2);	let map = Array.from(parser(input_string).keys());	console.log(parser((input_string)));	console.log("Reduced form:", reducer(parser(input_string), process.argv.slice(2).join('').toUpperCase()));	let pol_degree = Math.max.apply(null, map);	console.log("Polinomial degree:", pol_degree);	if (pol_degree > 2) {		console.log("The polynomial degree is stricly greater than 2, I can't solve.");		process.exit(0);	}	let a = parser(input_string).get(2);	let b = parser(input_string).get(1);	let c = parser(input_string).get(0);	quadratic_equation_solver(a, b, c);};if (require.main === module){	main(process.argv.slice(2));}