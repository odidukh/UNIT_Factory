const quadratic_equation_solver = require("./solver");const parser = require("./parser");if (process.argv.length !== 3) {	console.log("Usage: ./computor \"equation\"");	console.log("Example: ./computor \"1 * X^2 + 2 * X^1 + 3 * X^0 = 0\"");	process.exit(0);}function reduced_form_natural(map) {	let reduced_equation;	if (map.get(0)) {			reduced_equation = map.get(0).toString()	}	if (map.get(1)) {		if (map.get(1) > 0) {			reduced_equation = reduced_equation + '+' + map.get(1).toString() + '*' + 'X';		} else {			reduced_equation = reduced_equation + map.get(1).toString() + '*' + 'X';		}	}	if (map.get(2)) {		if (map.get(2) > 0) {			reduced_equation = reduced_equation + '+' + map.get(2).toString() + '*' + 'X^2=0';		} else {			reduced_equation = reduced_equation + map.get(2).toString() + '*' + 'X^2=0';		}	}	return (reduced_equation.split('').join(' ').replace(" ^ ", '^'));}const main = (args) => {	console.log(parser());	let map = Array.from(parser().keys());	console.log("Reduced form:", reduced_form_natural(parser()));	let pol_degree = Math.max.apply(null, map);	console.log("Polinomial degree:", pol_degree);	if (pol_degree > 2) {		console.log("The polynomial degree is stricly greater than 2, I can't solve.");		process.exit(0);	}	let a = parser().get(2);	let b = parser().get(1);	let c = parser().get(0);	quadratic_equation_solver(a, b, c);};if (require.main === module){	main(process.argv.slice(2));}